---
name: film-storyboard-skill
description: 影视分镜生成技能。将剧本转化为分镜产出，采用分层渐进式流程：节拍拆解 → 九宫格 → 动态分镜板（支持可变格子数1-4格）。
---

# 影视分镜生成 Skill

[技能说明]
    影视分镜生成技能，采用模块化架构。
    流程：节拍拆解 → 九宫格关键帧选集 → 四宫格段落展开。
    遵循 storyboard-methodology-playbook/ 中的环境深度构造规则。
    提示词采用叙事描述式（完整段落），而非关键词堆叠式。
    【新增】调用创意引擎系统（蒙太奇逻辑引擎、影视联想引擎、通感视觉化引擎）
    【新增】记录引擎输出和置信度，支持阶段间继承
    【新增】调用环境构造系统，构建沉浸式场景
    【V3.0新增】支持多结构叙事模板（经典三幕式、英雄之旅、起承转合、多巴胺闭环）
    【V3.0新增】集成潜台词分析系统，挖掘角色心理动机
    【V3.0新增】使用类型化戏剧权重计算算法，支持影片类型修正

[自动加载模块] - 按顺序加载执行

    ## 节拍拆解阶段 (breakdown)
    ┌─────────────────────────────────────────────────────────────────┐
    │ 步骤1: 读取项目配置                                             │
    │  - .agent-state.json → visual_style, target_medium, aspect_ratio│
    │  - 【V3.0新增】读取 narrative_structure, genre, enable_* 配置   │
    └─────────────────────────────────────────────────────────────────┘
                                ↓
    ┌─────────────────────────────────────────────────────────────────┐
    │ 步骤2: 读取输入文件                                             │
    │  - 剧本文件：script/*-{集数}.md                                 │
    │  - 导演参数：common/director-decision.md (如存在)              │
    └─────────────────────────────────────────────────────────────────┘
                                ↓
    ┌─────────────────────────────────────────────────────────────────┐
    │ 步骤3: 读取分析模块                                             │
    │  - common/beat-analyzer.md ← 【核心模块】节拍分析逻辑          │
    │  - film-storyboard-skill/templates/beat-breakdown-template.md ← 输出格式模板         │
    │  - film-storyboard-skill/creative-engines-integration/ ← 创意引擎系统 │
    │  - 【V3.0新增】common/structure-profiles.md ← 结构模板选择     │
    │  - 【V3.0新增】common/subtext-analyzer.md ← 潜台词分析         │
    └─────────────────────────────────────────────────────────────────┘
                                ↓
    ┌─────────────────────────────────────────────────────────────────┐
    │ 步骤4: 执行节拍分析                                             │
    │  按 beat-analyzer.md 规则执行：                                 │
    │  - 【V3.0更新】根据 narrative_structure 选择结构模板           │
    │  - 【V3.0更新】使用类型化戏剧权重计算算法（考虑genre修正）     │
    │  - 【V3.0新增】执行潜台词分析，挖掘角色心理动机                │
    │  - 节拍数量：8-12个为宜                                         │
    │  - 每个节拍必须包含：场景描述、角色行动、情感表达               │
    │  - 计算综合权重和关键帧等级                                     │
    └─────────────────────────────────────────────────────────────────┘
                                ↓
    ┌─────────────────────────────────────────────────────────────────┐
    │ 步骤5: 执行创意引擎分析 ← 【新增】                              │
    │  调用 creative-engines-integration/ 模块：                      │
    │  - 蒙太奇逻辑引擎：分析戏剧权重、镜头拆解建议、三段式拆解       │
    │  - 影视联想引擎：检索匹配影片、提取视觉DNA                      │
    │  - 通感视觉化引擎：识别感官刺激、转化视觉符号                   │
    │  - 输出：引擎分析报告（含置信度评分）                           │
    └─────────────────────────────────────────────────────────────────┘
                                ↓
    ┌─────────────────────────────────────────────────────────────────┐
    │ 步骤6: 执行质量自检                                             │
    │  - 读取 common/quality-check.md                                 │
    │  - 按清单逐项检查                                               │
    │  - 不通过则修正后重新检查                                       │
    └─────────────────────────────────────────────────────────────────┘
                                ↓
    ┌─────────────────────────────────────────────────────────────────┐
    │ 步骤7: 保存输出                                                 │
    │  - outputs/beat-breakdown-{集数}.md                            │
    │  包含：节拍拆解表 + 附录: 创意引擎分析                          │
    └─────────────────────────────────────────────────────────────────┘

    ## 九宫格提示词阶段 (beatboard)
    ┌─────────────────────────────────────────────────────────────────┐
    │ 步骤1: 读取节拍拆解表                                           │
    │  - outputs/beat-breakdown-{集数}.md                            │
    │  - 读取 附录: 创意引擎分析 部分                                 │
    │  - 提取每个节拍的"关键信息点"字段（JSON格式）                   │
    │  - 提取每个节拍的"辅助信息"字段（如有）                         │
    └─────────────────────────────────────────────────────────────────┘
                                ↓
    ┌─────────────────────────────────────────────────────────────────┐
    │ 步骤2: 读取关键帧选择模块                                       │
    │  - common/keyframe-selector.md                                  │
    └─────────────────────────────────────────────────────────────────┘
                                ↓
    ┌─────────────────────────────────────────────────────────────────┐
    │ 步骤3: 执行关键帧选择                                           │
    │  按 keyframe-selector.md 规则执行：                             │
    │  - 根据节拍权重和关键帧等级选择核心画面                         │
    │  - 每个板选择9个格子覆盖节拍范围                                │
    └─────────────────────────────────────────────────────────────────┘
                                ↓
    ┌─────────────────────────────────────────────────────────────────┐
    │ 步骤4: 读取环境构造模块 ← 【新增】                              │
    │  - film-storyboard-skill/environment-construction-guide/        │
    │    - 空间骨架构建                                               │
    │    - 四层结构（前景/中景/背景/天空）                            │
    │    - 材质定义                                                   │
    │    - 光影基调                                                   │
    │    - 动态元素                                                   │
    └─────────────────────────────────────────────────────────────────┘
                                ↓
    ┌─────────────────────────────────────────────────────────────────┐
    │ 步骤5: 读取布局计算模块                                         │
    │  - common/layout-calculator.md                                  │
    └─────────────────────────────────────────────────────────────────┘
                                ↓
    ┌─────────────────────────────────────────────────────────────────┐
    │ 步骤6: 读取九宫格模板                                           │
    │  - film-storyboard-skill/templates/beat-board-template.md       │
    └─────────────────────────────────────────────────────────────────┘
                                ↓
    ┌─────────────────────────────────────────────────────────────────┐
    │ 步骤7: 执行环境构造 ← 【新增】                                  │
    │  为每个格子调用 environment-construction-guide：                │
    │  - 空间骨架：基础布局、关键物体位置、角色活动范围               │
    │  - 四层结构：前景层、中景层、背景层、天空层                     │
    │  - 材质：表面特征、纹理细节                                     │
    │  - 光影基调：光源类型、色彩氛围                                 │
    │  - 动态元素：需要动态效果的对象                                 │
    └─────────────────────────────────────────────────────────────────┘
                                ↓
    ┌─────────────────────────────────────────────────────────────────┐
    │ 步骤8: 生成九宫格提示词 ← 【增强】                              │
    │  为每个格子生成：                                               │
    │  - 环境构造（整合空间骨架、四层结构、材质、光影基调、动态元素）│
    │  - 画面描述（叙事描述式）                                       │
    │  - 镜头语言                                                     │
    │  - 风格标签                                                     │
    │  - 情绪基调                                                     │
    │  - **置信度**：继承引擎分析的置信度，<60%标记低置信度           │
    │  - **引擎输出摘要**：蒙太奇逻辑+影视联想分析摘要                │
    │  【提示词写法】遵循 gemini-image-prompt-guide/ 中的规范         │
    └─────────────────────────────────────────────────────────────────┘
                                ↓
    ┌─────────────────────────────────────────────────────────────────┐
    │ 步骤9: 执行连贯性检查                                           │
    │  - 读取 common/coherence-checker.md                             │
    └─────────────────────────────────────────────────────────────────┘
                                ↓
    ┌─────────────────────────────────────────────────────────────────┐
    │ 步骤10: 保存输出                                                │
    │  - outputs/beat-board-prompt-{集数}-board*.md                   │
    │  每个格子包含：环境构造 + 置信度 + 引擎输出摘要                 │
    └─────────────────────────────────────────────────────────────────┘

      ## 动态分镜板生成阶段 (sequence) ← 【V4.0更新】
      ┌─────────────────────────────────────────────────────────────────┐
      │ 步骤1: 读取节拍拆解表 ← 【V4.0新增】                            │
      │  - outputs/beat-breakdown-{集数}.md                            │
      │  - 提取每个节拍的"核心策略"字段（含策略标签）                   │
      │  - 提取"复杂度评分"和"建议格子数"                               │
      │  - 提取每个节拍的"关键信息点"字段（JSON格式）                   │
      │  - 提取每个节拍的"辅助信息"字段（如有）                         │
      └─────────────────────────────────────────────────────────────────┘
                                 ↓
     ┌─────────────────────────────────────────────────────────────────┐
     │ 步骤2: 读取九宫格提示词                                         │
     │  - outputs/beat-board-prompt-{集数}-board*.md                   │
     │  - 提取每个格子的环境构造、置信度和引擎输出摘要                 │
     └─────────────────────────────────────────────────────────────────┘
                                 ↓
     ┌─────────────────────────────────────────────────────────────────┐
     │ 步骤3: 读取导演审核报告                                         │
     │  - outputs/beat-board-prompt-{集数}-review.md                   │
     │  - 提取导演审核意见                                             │
     └─────────────────────────────────────────────────────────────────┘
                                 ↓
      ┌─────────────────────────────────────────────────────────────────┐
      │ 步骤4: 调用动态分镜引擎 ← 【V4.0新增】                          │
      │  - 读取 common/dynamic-breakdown-engine.md                       │
      │  - 执行四阶段流程：                                             │
      │    ① 策略映射：将核心策略标签映射到格子数分配                   │
      │    ② 粒度评估：基于复杂度评分确定格子密度                       │
      │    ③ 跨板协调：处理跨板节拍的连续性                             │
      │    ④ 完整性校验：检查台词/动作/视觉的完整覆盖                   │
      │  - 输出：每个节拍的实际格子数（1-4格）                         │
      │                                                                │
      │  **冲突处理机制**：                                             │
      │  - 优先级1：核心策略基础格子数                                  │
      │    • 单镜 → 1格                                                │
      │    • 动势组 → 2格                                              │
      │    • 蒙太奇组 → 3格                                            │
      │    • 长镜头 → 1-3格                                            │
      │  - 优先级2：复杂度调整                                          │
      │    • 调整后格子数 = 基础格子数 + max(0, round(复杂度评分 - 2.5))│
      │    • 限制条件：调整后格子数 ≤ 4                                │
      │  - 优先级3：关键点映射                                          │
      │    • Setup → 第1格                                             │
      │    • Action → 第2-3格（动势组第2格，蒙太奇组第2-3格）          │
      │    • Resolution → 第3格（动势组第2格，蒙太奇组第3格）           │
      └─────────────────────────────────────────────────────────────────┘
                                 ↓
     ┌─────────────────────────────────────────────────────────────────┐
     │ 步骤5: 读取动态分镜板模板 ← 【V4.0更新】                         │
     │  - film-storyboard-skill/templates/sequence-board-template.md   │
     │  - 使用协议1.0：四宫格裂变协议                                  │
     └─────────────────────────────────────────────────────────────────┘
                                 ↓
      ┌─────────────────────────────────────────────────────────────────┐
      │ 步骤6: 生成继承记录                                             │
      │  为每个继承的格子记录：                                         │
      │  - 原导演审核意见                                               │
      │  - 环境构造继承（空间骨架、四层结构、材质、光影基调、动态元素）│
      │  - 引擎输出摘要                                                 │
      │  - 置信度                                                       │
      │  - 低置信度标记（<60%）                                         │
      │  - 【V4.0新增】策略标签（[单镜]/[动势组]/[蒙太奇组]/[长镜头]）│      └─────────────────────────────────────────────────────────────────┘
                                 ↓
      ┌─────────────────────────────────────────────────────────────────┐
      │ 步骤7: 生成动态分镜板提示词 ← 【V4.0更新】                       │
      │  继承九宫格要素并按策略展开：                                   │
      │  - 根据策略标签分配格子数（1-4格）                             │
      │  继承九宫格要素并按策略展开：                                   │
      │  - 根据策略标签分配格子数（1-4格）                             │
      │  - 每个镜头继承环境构造                                         │
      │  - 每个镜头继承置信度和引擎输出摘要                             │
      │  - 记录本次修改/优化                                            │
      │  - 新增引擎分析（如有）                                         │
      │  - 处理跨板连续性（动势组/蒙太奇组/长镜头）                     │
      │  - 自动创建新板（当节拍需要跨板展开时）                         │
      │  【提示词写法】遵循 gemini-image-prompt-guide/ 中的规范         │     ┌─────────────────────────────────────────────────────────────────┐
     │ 步骤8: 执行连贯性检查 ← 【V4.0更新】                            │
     │  - 读取 common/coherence-checker.md                             │
     │  - 检查跨板连贯性（动势组/蒙太奇组/长镜头的连续性）             │
     │  - 验证策略一致性                                               │
     └─────────────────────────────────────────────────────────────────┘
                                 ↓
     ┌─────────────────────────────────────────────────────────────────┐
     │ 步骤9: 保存输出                                                 │
     │  - outputs/sequence-board-prompt-{集数}-board*.md ← 【V4.0更新】│
     │  - 支持多个板（根据策略自动创建）                               │
     │  - 包含：继承记录章节 + 动态分镜板提示词（1-4格）               │
     │  - 板与板之间通过"跨板连续性"标记连接                           │
     └─────────────────────────────────────────────────────────────────┘

[环境构造规范] ← 【新增】

    ## 九宫格阶段（静态环境构造）
    | 构造元素 | 内容 | 详细程度 |
    |----------|------|----------|
    | 空间骨架 | 基础布局、物体位置、角色活动范围 | 中 |
    | 四层结构 | 前景层、中景层、背景层、天空层 | 高 |
    | 材质 | 表面特征、纹理细节、物理属性 | 中 |
    | 光影基调 | 光源类型、色彩氛围 | 中 |
    | 动态元素 | 需要动态效果的对象类型 | 中 |

    ## 动态提示词阶段（动态效果细化）← 【新增】
    | 动态元素 | 内容 | 详细程度 |
    |----------|------|----------|
    | 光影运动 | 光线移动、阴影渐变、色彩渐变 | 高 |
    | 动态运动 | 元素的具体运动方式、物理效果 | 高 |
    | 节奏同步 | 与音频/角色动作配合 | 高 |

[引擎输出规范] ← 【新增】

    ## 引擎调用时机
    | 阶段 | 调用引擎 | 输出位置 |
    |------|----------|----------|
    | 节拍拆解 | 全部3个引擎 | beat-breakdown-{集数}.md 附录 |
    | 九宫格 | 继承节拍拆解结果 | beat-board-prompt-{集数}-board*.md 每格 |
    | 四宫格 | 继承九宫格结果 | sequence-board-prompt-{集数}.md 继承记录 |

    ## 置信度管理
    | 置信度范围 | 标记 | 处理方式 |
    |------------|------|----------|
    | ≥80% | 高置信度 | 正常输出 |
    | 60-79% | 中置信度 | 正常输出，提示可优化 |
    | <60% | 低置信度 | 标记"低置信度，需重新评估" |

    ## 引擎输出继承规则
    1. 节拍拆解 → 九宫格：继承蒙太奇逻辑、影视联想、通感转化结果
    2. 九宫格 → 四宫格：继承导演审核意见、引擎输出摘要、置信度
    3. 低置信度项：必须在每个阶段显式标记

 [输出文件]
     | 任务类型 | 输出路径 |
     |----------|----------|
     | 节拍拆解 | outputs/beat-breakdown-{集数}.md |
     | 九宫格 | outputs/beat-board-prompt-{集数}-board*.md |
     | 动态分镜板 | outputs/sequence-board-prompt-{集数}-board*.md ← 【V4.0更新】 |
     | 自查报告 | outputs/{类型}-prompt-{集数}-self-check.md |

[执行约束]
    ## 节拍拆解阶段约束（必须遵守）
    
    ### 数量约束
    - 节拍数量：8-12个为宜
    - 特级关键帧（🔴）：1-3个
    - 高级关键帧（🟠）：2-4个
    - 中级关键帧（🟡）：3-5个
    
    ### 三幕结构约束
    | 幕次 | 比例 | 内容要求 |
    |------|------|----------|
    | 第一幕 | 15-25% | 世界观建立、激励事件 |
    | 第二幕 | 50-70% | 发展、转折、中点、高潮前集结 |
    | 第三幕 | 15-25% | 高潮对抗、结局收尾 |
    
    ### 节拍内容约束（每个节拍必须包含）
    - 场景描述：发生了什么（最小叙事单元）
    - 角色行动：角色的具体动作和意图
    - 情感表达：场景传递的核心情绪
    - 叙事功能：世界观建立/诱因事件/高潮对抗等
    - 节拍类型：动作节拍/情绪转折/氛围转换/重要对话/过渡节拍
    
    ### 质量关卡
    生成后必须通过 quality-check.md 自检：
    □ 三幕比例符合标准
    □ 节拍数量在 8-12 范围内
    □ 叙事完整性（有开篇、发展、高潮、结局）
    □ 每个节拍有明确的场景描述
    □ 每个节拍有清晰的角色行动
    □ 每个节拍有具体的情感表达
    □ 【V3.0新增】结构符合度检查（根据所选叙事结构）
    □ 【V3.0新增】戏剧权重合理性检查（使用新算法）
    □ 【V3.0新增】潜台词分析检查（如启用）
    □ 【V3.0新增】V3.0功能启用检查（配置正确性）

[角色设定提取机制] ← 【新增】

    ## YAML 区块读取方法
    当剧本文件包含 YAML 区块时（如 script/猪打镇关西_v2.0），按以下规则提取：
    
    ### 提取步骤
    1. 定位剧本文件开头的 YAML 区块（```yaml ... ```）
    2. 提取以下关键信息：
       - characters: 角色外观设定（头饰、服装、配饰）
       - scenes: 场景描述细节
       - rendering_styles: 渲染风格定义（国潮动漫、MCU、像素等）
       - style_transitions: 风格转换点
    
    ### 角色设定工具模块
    可使用 configs/character-loader.py 工具模块辅助提取：
    - extract_character_settings() - 提取 YAML 区块中的角色设定
    - get_character_info(character_name, render_style) - 获取特定角色的详细信息
    
    ### 角色设定应用规则
    在生成节拍拆解时，必须使用提取的角色设定：
    
    1. **角色外观引用**
       - 引用头饰信息：{character_info['headwear']}
       - 引用服装信息：{character_info['upper_body']}
       - 引用配饰信息：{character_info['accessories']}
    
    2. **渲染风格应用**
       - 引用渲染风格：{character_info['render_style']}
       - 在场景描述中体现风格特征
    
    3. **场景细节使用**
       - 引用场景描述：{scene_info['description']}
       - 使用标准化的场景细节表述
    
    4. **风格转换点处理**
       - 识别风格转换点（如节拍8、11）
       - 在相应节拍中明确标注风格变化
  
 [参考文件]
     - 完整规范：见 common/ 目录下各模块文件
     - 模板文件：见 templates/ 目录
     - 方法论：见 storyboard-methodology-playbook/
     - 提示词写法：见 gemini-image-prompt-guide/
     - 创意引擎：见 creative-engines-integration/
     - 环境构造：见 environment-construction-guide/
     - 【V3.0新增】结构模板：common/structure-profiles.md
     - 【V3.0新增】潜台词分析：common/subtext-analyzer.md
     - 【V3.0新增】权重算法：common/beat-analyzer.md（已更新）
     - 【V4.0新增】动态分镜引擎：common/dynamic-breakdown-engine.md

[注意事项]
    - 四宫格必须继承九宫格对应格的所有要素
    - 发生变化必须显式说明原因（换装、受伤、天气变化等）
    - 默认视觉风格为真人风格，可被用户覆盖
    - 【新增】必须调用创意引擎系统，记录引擎输出
    - 【新增】每个格子必须记录置信度，<60%需标记
    - 【新增】四宫格必须继承导演审核意见和引擎输出
    - 【新增】九宫格必须调用环境构造系统，构建沉浸式场景
    - 【V3.0新增】必须根据配置选择正确的叙事结构模板
    - 【V3.0新增】必须使用类型化戏剧权重计算算法
    - 【V3.0新增】如启用潜台词分析，必须执行并记录结果
    - 【V3.0新增】必须验证V3.0功能配置正确性

---

## [详细执行指南] ← 【新增】

以下文件提供详细的分步执行指南，供在不清楚具体步骤时查阅：

### 参考文件

**common/ 目录 - 核心模块**
- [beat-analyzer.md](common/beat-analyzer.md) - 节拍分析逻辑（已更新V3.0）
- [keyframe-selector.md](common/keyframe-selector.md) - 关键帧选择规则
- [layout-calculator.md](common/layout-calculator.md) - 布局计算逻辑
- [coherence-checker.md](common/coherence-checker.md) - 连贯性检查
- [quality-check.md](common/quality-check.md) - 质量检查清单（已更新V3.0）
- [data-validator.md](common/data-validator.md) - 数据验证工具
- 【V3.0新增】[structure-profiles.md](common/structure-profiles.md) - 多结构叙事模板
- 【V3.0新增】[subtext-analyzer.md](common/subtext-analyzer.md) - 潜台词分析系统

**templates/ 目录 - 输出模板**
- [beat-breakdown-template.md](film-storyboard-skill/templates/beat-breakdown-template.md) - 节拍拆解表模板
- [beat-board-template.md](film-storyboard-skill/templates/beat-board-template.md) - 九宫格提示词模板
- [sequence-board-template.md](film-storyboard-skill/templates/sequence-board-template.md) - 四宫格提示词模板

**methodology/ 目录 - 方法论**
- [storyboard-methodology-playbook/](film-storyboard-skill/storyboard-methodology-playbook/) - 完整的分镜方法论

**guides/ 目录 - 指南**
- [gemini-image-prompt-guide/](film-storyboard-skill/gemini-image-prompt-guide/) - Gemini 提示词写法指南

**docs/executors/ 目录 - 详细执行指南（备用）** ← 【新增】
- [docs/executors/breakdown-executor.md](docs/executors/breakdown-executor.md) - 节拍拆解详细步骤（备用）
- [docs/executors/beatboard-executor.md](docs/executors/beatboard-executor.md) - 九宫格生成详细步骤（备用）
- [docs/executors/sequence-executor.md](docs/executors/sequence-executor.md) - 四宫格生成详细步骤（备用）
- [docs/executors/call-examples.md](docs/executors/call-examples.md) - 完整调用示例（备用）

### 备用机制

**异常处理机制** ← 【新增】
- [exception-handler.md](common/exception-handler.md) - 异常处理与容错机制（遇异常时读取）

**文件命名规范** ← 【新增】
- [AGENTS.md](AGENTS.md) - 输出文件命名规范（见指令路由表）

**参考文件总结**：
- 核心模块：common/ 目录（9个文件，含V3.0新增）
- 输出模板：templates/ 目录（3个文件）
- 方法论：storyboard-methodology-playbook/ 目录
- 指南：gemini-image-prompt-guide/ 目录
- 创意引擎：creative-engines-integration/ 目录
- 环境构造：environment-construction-guide/ 目录
- 详细指南：docs/executors/ 目录（5个文件）
- 异常处理：exception-handler.md
- 文件规范：见 AGENTS.md 指令路由表
- 【V3.0新增】结构模板：structure-profiles.md
- 【V3.0新增】潜台词分析：subtext-analyzer.md

---

