---
description: 执行四宫格提示词阶段。基于九宫格提示词生成 Sequence Board 四宫格提示词。完整支持模块依赖、导演审核、用户确认和快照管理。
---

# 四宫格提示词

**目的**：基于九宫格提示词生成 Sequence Board 四宫格提示词

**前置条件**：
- 九宫格提示词已完成：`outputs/beat-board-prompt-{集数}-board*.md`

**使用方式**：
```bash
/sequence           # 单集时使用
/sequence ep01      # 指定集数
```

**输出文件**：
- `outputs/sequence-board-prompt-{集数}.md`

**结构说明**：
- 将所有节拍组织为多个四宫格组（每组4个镜头）
- 每组有明确的叙事功能和情绪定位
- 包含风格转换说明和人物一致性参考

**完整工作流程**：

```
步骤1: 读取前置产物
       ├── outputs/beat-board-prompt-{集数}.md（九宫格总览）
       ├── outputs/beat-board-prompt-{集数}-board*.md（九宫格分板）
       └── .agent-state.json

步骤2: 读取分析模块（按 film-storyboard-skill/SKILL.md 定义）
       ├── common/coherence-checker.md ← 连贯性检查
       └── templates/sequence-board-template.md ← 输出格式模板

步骤3: 继承九宫格要素
       ├── 读取每个九宫格分板的详细描述
       ├── 每个四宫格必须继承对应九宫格格子的：
       │   ├── 人物外观/服装状态
       │   ├── 场景要素/环境设定
       │   └── 光色基调/氛围
       └── 变化必须显式说明原因

步骤4: 生成四宫格提示词
       ├── 按 sequence-board-template.md 格式生成
       ├── 起承转合结构（起→承→转→合）
       ├── 每格包含：镜头类型、画面要素、情绪基调
       └── 动作戏结构（预备→出手→命中/受击→反应/余势）

步骤5: 执行连贯性检查
       ├── 按 coherence-checker.md 逐项检查：
       │   ├── 与九宫格的一致性继承
       │   ├── 屏幕方向/视线/轴线稳定
       │   ├── 动作连续性
       │   └── 剪接风险检查
       └── 不通过则修正后重新检查

步骤6: 保存输出文件
       └── outputs/sequence-board-prompt-{集数}.md

步骤7: 调用导演审核（storyboard-review-skill）
       ├── 读取 storyboard-review-skill/review-checklist.md
       ├── 读取 storyboard-review-skill/asset-consistency-rules.md（如需要）
       ├── 按 Sequence Board 验收清单逐项检查
       └── 生成审核报告：outputs/sequence-board-prompt-{集数}-review.md

步骤8: 执行用户确认（CP3）
       ├── 读取 common/user-confirmation-protocol.md
       ├── 展示标准确认UI：
       │   ├── 进度条
       │   ├── 质量评分
       │   ├── 审核状态
       │   └── 操作选项：[通过] ✅ / [修改] ✏️ / [重生成] 🔄 / [回滚] ⏪
       └── 等待用户选择

步骤9: 创建快照（按 common/snapshot-management.md）
       ├── 快照类型：分组快照
       ├── 保存路径：.strategic-snapshots/snapshot-{编号}/
       └── 更新 .agent-state.json

步骤10: 进入下一阶段或结束
         ├── [通过] → 可执行 /motion 进入动态提示词阶段
         ├── [修改] → 等待修改后重新审核
         ├── [重生成] → 返回步骤3重新执行
         └── [回滚] → 恢复到历史快照
```

**模块依赖声明**（来自 film-storyboard-skill/SKILL.md）：

| 模块 | 作用 | 是否必需 |
|------|------|----------|
| common/coherence-checker.md | 连贯性检查 | ✅ 必需 |
| templates/sequence-board-template.md | 输出格式模板 | ✅ 必需 |

**协议文件**：

| 文件 | 作用 | 调用时机 |
|------|------|----------|
| common/user-confirmation-protocol.md | 用户确认UI规范 | 步骤8执行前 |
| common/snapshot-management.md | 快照创建规则 | 步骤9执行前 |

**相关文件**：
- 模板：`film-storyboard-skill/templates/sequence-board-template.md`
- 连贯性检查：`common/coherence-checker.md`
- 审核：`storyboard-review-skill/`（使用 review-checklist.md）
- 协议：`common/user-confirmation-protocol.md`、`common/snapshot-management.md`
