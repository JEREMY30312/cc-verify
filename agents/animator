#!/bin/bash

# ANINEO Agent 快捷调用 - Animator (动画师)

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
CALLER="$SCRIPT_DIR/caller.js"

TASK="${1:-default}"
shift

echo "🎬 调用 animator (动画师), 任务: $TASK"
node "$CALLER" animator --task "$TASK" "$@"
